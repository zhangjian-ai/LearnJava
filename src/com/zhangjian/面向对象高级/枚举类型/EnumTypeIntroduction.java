package com.zhangjian.面向对象高级.枚举类型;

/**
 * 枚举类型：
 *    1、枚举对应英文 enumeration，简写为 enum
 *    2、枚举类型属于一种特殊的类，里面只包含了一组有限的 特定对象(这些对象就是当前枚举类型的实例)
 *    3、枚举类型中的这些特定对象，一般都是 final static 修饰的，外部可通过类名直接访问
 *    4、枚举类型 主要价值在于约束程序员不能随意实例化某个类，而只能使用该类中默认提供的几种实例。
 *       例如 Season 类，它的实例就应该只有 SPRING、WINTER、SUMMER、AUTUMN 四个
 *
 * 枚举类的实现方式：
 *    1、自定义枚举类
 *    2、使用 enum关键字 实现枚举
 */
public class EnumTypeIntroduction {
}
